import styled from "@emotion/styled";

export const Grid = styled.section`
  display: flex;
  background: white;
  justify-content: center;
  align-items: center;
  flex-flow: row wrap;
`;

export default Grid;
